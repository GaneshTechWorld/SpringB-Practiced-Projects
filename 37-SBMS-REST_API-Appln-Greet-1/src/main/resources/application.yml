server:
  port: 8764  
spring:
  application:
    name: GREET-API
  boot:
    admin:
      client:
        url: http://localhost:8762/  
eureka:
  client:
    service-url:
       defaultZone: http://localhost:8761/eureka/
       
management:
  endpoints:
    web:
      exposure:
        include: '*'